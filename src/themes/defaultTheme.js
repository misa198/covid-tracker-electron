import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d84315',
    },
    background: {
      default: '#fff',
    },
    text: {
      primary: '#333',
    },
  },
});

export default theme;
